@model ProductListModel

<div class="row">
    <div class="col-md-12">
        <div class="row mt-3">
            @foreach (var product in Model.Products)
            {
             <partial name="_product" for="@product" />
            }
        </div>
    </div>
</div>